// ===== MODULE: spawner =====
// STUB — arrives at step 6 (Architecture §3.5, §9.6).
// Will read the race config's hazard tables and elevation profile and emit obstacles, animals, pickups
// and weather with seeded RNG (seed = race id + attempt). Until then, placeholder trail pickups live in
// sim.js (pickupAt) and weather windows in atmosphere.js (atmoStep).
export {};
